/**
 * OpenAI voice id for the teacher (Realtime) and listening-modal TTS.
 * Keep in sync with the `voice` field in `src/app/api/realtime/token/route.ts`.
 */
export const OPENAI_LESSON_VOICE = 'sage';
